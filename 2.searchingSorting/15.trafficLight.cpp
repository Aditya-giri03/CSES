#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, n;
    cin >> x >> n;
    vector<int> lights;
    for (int i = 0; i < n; i++)
    {
        int val;
        cin >> val;
        lights.push_back(val);
    }

    return 0;
}